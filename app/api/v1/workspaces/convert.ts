import { WorkspaceLifecycleController } from '@controllers/WorkspaceLifecycleController'; import { convertWorkspaceSchema } from '@schemas/workspaceLifecycle'; import { workspacePublicIdSchema } from '@schemas/workspace'; import { getRequestMetadata, parseJson } from '@utils/request'; import { resp } from '@qubitcodes/qcresp';
export async function action({params,request}:{params:{workspaceId?:string};request:Request}):Promise<Response>{const id=workspacePublicIdSchema.safeParse(Number(params.workspaceId));if(!id.success)return resp.failure('Workspace not found.',resp.codes.RESOURCE_NOT_FOUND,undefined,null,undefined,404);const input=await parseJson(request,convertWorkspaceSchema);return input instanceof Response?input:WorkspaceLifecycleController.convert(request,id.data,input,getRequestMetadata(request));}
