import { ArrowUpRight, KeyRound, Server, ShieldCheck } from 'lucide-react';
import { Link } from 'react-router';

/** Honest Phase 1 landing page before infrastructure resources are connected. */
export default function OverviewPage() {
	return <div className="mx-auto max-w-7xl"><section className="overflow-hidden rounded-[2rem] bg-brand-primary p-6 text-white sm:p-9"><p className="text-sm font-semibold text-brand-action">Platform foundation</p><h2 className="mt-3 max-w-2xl text-3xl font-semibold tracking-[-.035em] sm:text-5xl">Secure access is ready. Infrastructure comes next.</h2><p className="mt-4 max-w-2xl text-white/75">Passwordless authentication, administrator access control, and device-session visibility now form the operating base.</p></section><div className="mt-6 grid gap-4 md:grid-cols-3">{[{ icon: ShieldCheck, title: 'Administrators', text: 'Roles, overrides, and security history', to: '/admin/administrators' }, { icon: KeyRound, title: 'Session security', text: 'Label and revoke signed-in devices', to: '/settings/sessions' }, { icon: Server, title: 'Hosting resources', text: 'Scheduled for the next implementation phase', to: '/admin/overview' }].map(({ icon: Icon, ...card }) => <Link className="group rounded-3xl border border-stone-200 bg-app-surface p-5 transition hover:-translate-y-0.5 hover:border-brand-action dark:border-stone-800" key={card.title} to={card.to}><Icon className="size-6 text-brand-primary dark:text-brand-action" /><div className="mt-7 flex items-end justify-between gap-4"><div><h3 className="font-semibold">{card.title}</h3><p className="mt-1 text-sm text-stone-500 dark:text-stone-400">{card.text}</p></div><ArrowUpRight className="size-5 text-stone-400 transition group-hover:text-brand-action" /></div></Link>)}</div></div>;
}
